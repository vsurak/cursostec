#include "Flight.h"
#include "../../starticketcr/generic/List.h"


// debe llenar una lista de pCantidad vuelos aleatorios cuya hora y minutos de partida esten entre pStartHour y pEndHour
// tomando en cuenta las restricciones de los minutos de salida segun el ejercicio 3 del caso #3
void fillFlights(int pCantidad, int pStartHour, int pEndHour) {
    // quiz #6, aqui es basicamente declarar una lista o un vector de Flight
    // List<Fligh> vuelos o vector<Flight>
    // tener dos constantes para hora de inicio y hora final
    // tener alguna lista para rellenar numeros y nombres y cantidades de vuelos aleatorios
    // escoger q el minuto de salida sea un random de 0 a 43 para respetar los 17 minutos de retraso
    // meter los vuelos a la lista
} 


int main() {
    Flight vuelo(4,35);

    Flight otroVuelo(14,20);
}