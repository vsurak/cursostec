-- securables -> objetos o items asegurables
-- usuarios, roles, tablas, vistas, stored procedures, funciones, servidores

-- granularidad
-- dentro de un securable que tan profundo o detallado puedo especificar un permiso
-- ejemplo: stored procedure, granularidad de exec o no exec 
-- ejemplo: tabla, columna, es decir, puedo limitar cierta columna de la tabla

-- principio:
-- a) doy permiso a todo, y niego aquello que quiero restringir
-- b) niego todo y doy acceso a lo que necesito

-- lo ideal es crear roles: operador, recepci�n, logistica, gerencia

